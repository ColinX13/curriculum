/**
 * Call X - takes in 2 numbers, return their sum
 * @param {number} a
 * @param {number} b
 * @returns {boolean}
 */

const solution = (a, b) => {
  let sum = 0;
  sum = a + b;
  return sum;
};

module.exports = {
  solution,
};
